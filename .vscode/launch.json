{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch local agent",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/agent_local/"
        }
    ]
}
